# Base URL of the GuardBox backend API — no trailing slash.
# Vite exposes vars prefixed with VITE_ to the client bundle.
# When wiring API calls in App.jsx, read as: import.meta.env.VITE_GUARDBOX_API_URL
VITE_GUARDBOX_API_URL=
